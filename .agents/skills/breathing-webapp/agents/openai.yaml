interface:
  display_name: "Breathing Webapp"
  short_description: "Repo-specific patterns and workflows for breathing-webapp"
  default_prompt: "Use the breathing-webapp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true